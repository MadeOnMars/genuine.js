const express = require('express');
const router = express.Router();

// Example
/*
router.get('/this-is-the-old-url', (req, res) => {res.redirect(301, '/new-wesite-destination')});
*/

module.exports = router;
